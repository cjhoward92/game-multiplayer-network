# Multiplayer UDP Network

This is an example UDP network with a working client and server that could be used in video game online multiplayer. The implementation is in Rust.

See the `server` and the `client`.